var nombre = 'Daniel'
var apellido = 'Lineros'

var nombreMayuscula = nombre.toUpperCase()
console.log(nombreMayuscula)
var nombreMinuscula = nombre.toLowerCase()
console.log(nombreMinuscula)
var primeraLetra = nombre.charAt(0)
console.log(primeraLetra)
var cantidadLetras = nombre.length
console.log(cantidadLetras)
var nombreCompleto = nombre + ' ' + apellido
console.log(nombreCompleto)
var nombreInterpolacion = `${nombre.toUpperCase()} interpolada`
console.log(nombreInterpolacion)
var str = nombre.charAt(1) + nombre.charAt(2)
console.log(str)
var substr = nombre.substr(1, 2)
console.log(substr)
